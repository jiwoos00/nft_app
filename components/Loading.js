import LoadingButton from "@mui/lab/LoadingButton";

export default function Loading() {
  return (
    <div className="mypage-loading-btn">
      <LoadingButton loading size="large" />
    </div>
  );
}
